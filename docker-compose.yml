services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_NAME: cameras-api
      APP_ENV: local
      APP_PORT: "8080"
      HTTP_CLIENT_TIMEOUT: 10s
      CATALOG_CACHE_TTL: 15m
      STREAM_CACHE_TTL: 1m
      LOG_LEVEL: info
